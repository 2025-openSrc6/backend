# Values here are loaded by `wrangler pages dev` for local Cloudflare bindings.
# Copy this file to `.dev.vars` (which stays gitignored) and fill in the secrets.

# Cloudflare credentials (ask the infra owner)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_D1_ID=

# Optional: other envs the app expects at runtime
SUI_NETWORK=testnet
SUI_RPC_URL=https://fullnode.testnet.sui.io
SUI_PACKAGE_ID=
SUI_CAP_OBJECT_ID=

# Local fallback DB path is still respected when running `npm run dev`
DATABASE_URL=file:./delta.db
